# create a new repo locally

#git init
# copy index.html into this folder (or save it directly here)
#git add index.html
#git commit -m "Initial site"
# create repo on GitHub (you can also create it via the GitHub website)
#git remote add origin https://github.com/yhifny/yhifny.github.io.git
git push -u origin master
